<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.damian.kinematicscalculatorvs3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.StartActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.MenuActivity" />

        <activity android:name=".activities.KinematicsForwardValue" />

        <activity
            android:name=".activities.KinematicsForwardDraw"
            android:screenOrientation="portrait" />

        <activity android:name=".activities.KinematicsInverseVariablesConstant" />

    </application>


</manifest>
